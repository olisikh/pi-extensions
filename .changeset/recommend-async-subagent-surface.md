---
"@narumitw/pi-subagents": patch
---

Recommend the async-only workflow in the `/subagents` chooser and README while preserving all delegation methods as the compatibility default.
Document exact mode-specific tool surfaces and retain explicit blocking, consultation, and split lifecycle compatibility routes.
